// 가운데 글자 가져오기
function solution(s) {
  let length = s.length
  // return (length % 2 == 0) ? s[s.length]
}
const arr = [1,2,3,4] // slice(1, 3) 1 = length / 2 - 1   3 = 1 + 2
const arr1 = [1,2,3] // slice(1, 2)  1 = length / 2 - 1   2 = 1 + 1
// ceil로 강제로 올려줌