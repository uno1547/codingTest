//문제 9663번 N-queens
let fs = require('fs')
let input = fs.readFileSync('input.txt').toString()
let number = Number(input)
console.log(number)
//먼저 같은 행row(가로)에만 못놓는경우 퀸이아닌 차
let result = []
function queen(n) {
  if (n === ) {
    
  }
}

queen(0) //부터 시작 